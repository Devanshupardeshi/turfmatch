/**
 * DEPRECATED — use @/lib/bootstrap instead.
 * This file re-exports the bootstrap-managed Supabase client
 * to avoid breaking existing imports.
 */
export { supabase } from "./bootstrap/session-manager"
